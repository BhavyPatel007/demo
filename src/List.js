import React from 'react'

const List = () => {
    return (
        <div>
            {/* {Data.map(p => (
                <div key={p}>{p}</div>
            ))} */}
        </div>
    )
}

export default List
